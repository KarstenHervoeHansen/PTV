SUBDESIGN hdecode
(
	clk,syssel,da[10..0]:		INPUT;
	upint,genst:				OUTPUT;
)
VARIABLE
	hint:					DFF;

BEGIN
	hint.clk	= genst;
	hint.clrn	= !da6;
	hint.d		= vcc;
IF syssel THEN
	genst = (da[10..0]==1715);
ELSE
	genst = (da[10..0]==1727);
END IF;
	upint	= !hint;
END;

