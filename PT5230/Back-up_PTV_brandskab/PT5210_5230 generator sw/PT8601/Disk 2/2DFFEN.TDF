SUBDESIGN 2dffen
(
	clk,s1,s0,ena:			INPUT;
	s1b,s0b:				OUTPUT;
)
VARIABLE
	sa,sb:					DFF;

BEGIN
	sa.clk	= clk;
	sb.clk	= clk;
  IF ena==1 THEN 
	sa.d	= GND;
	sb.d	= GND;
   ELSE 
	sa.d	= s1;
	sb.d	= s0;
  END IF;
	s1b 	= sa.q;
	s0b 	= sb.q;
END;

