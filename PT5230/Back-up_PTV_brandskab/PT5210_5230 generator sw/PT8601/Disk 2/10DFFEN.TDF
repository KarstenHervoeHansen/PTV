SUBDESIGN 10dffen
(
	clk,ab[9..0],ena:			INPUT;
	dout[9..0]:					OUTPUT;
)
VARIABLE
	dout[9..0]:					DFF;

BEGIN
	dout[].clk	= clk;
  IF ena==1 THEN
	dout[].d	= 384;
   ELSE 
	dout[].d	= ab[];
   END IF;
END;

