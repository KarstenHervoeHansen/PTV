SUBDESIGN loadncoa
(
	crst,ldnco,ad8		: input;
	loadnco				: output;
)

	VARIABLE
	load 				: DFF;
	
	BEGIN
	load.clk  = ldnco;
	load.d	  = ad8;

	loadnco   = (crst & !load.q) # (load.q & ldnco);

END;
 

