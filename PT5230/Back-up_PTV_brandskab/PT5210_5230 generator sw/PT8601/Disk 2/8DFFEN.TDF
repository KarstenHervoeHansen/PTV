SUBDESIGN 8dffen
(
	clk,ab[7..0],ena:			INPUT;
	dout[7..0]:					OUTPUT;
)
VARIABLE
	dout[7..0]:					DFF;

BEGIN
	dout[].clk	= clk;

  IF ena==1 THEN
	dout[].d    = 0;   
   ELSE
	dout[].d	= ab[];
  END IF; 
END;

