SUBDESIGN palid
(
	clk,hload,reset,syssel		: input;
	palid	 					: output;
)

VARIABLE
    palid				: DFFE;   

	BEGIN

	palid.clk  = clk;
	palid.d	   = (reset # !palid) & !syssel;
	palid.ena  = hload;
%	palid.clrn = !reset;%

END;
 

