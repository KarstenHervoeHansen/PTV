SUBDESIGN dtoreset
(
	clk,preset,mente		: input;
	okflag,reset			: output;
)
VARIABLE

    hclrb			: DFF;   

	BEGIN

	hclrb.clk   = clk;
	hclrb.d	    = preset;

	okflag		= hclrb;
	reset 		= !(hclrb & !clk & !mente);
END;
 

