Usage: 
       lkxa [option]... [file]...

Summary of options:
       -C           case insensitive
       -H or ?      display options
       -L<dir>      additional search path for system libraries
       -L           skip system library search
       -M           produce a linker mapfile (.lnl)
       -N           turn off overlaying
       -O<name>     basename of the resulting map files
       -V           display version information
       -c           produce a compact call graph file (.cal)
       -d<file>     name of descriptor file, '-' means stdin
       -e           clean up if erroneous result
       -err         redirect errors to a file with extension .elk
       -f<file>     name of invocation file, '-' means stdin
       -l<x>        search also in system library lib<x>.a
       -o<name>     name of the resulting .out file
       -r           suppress undefined symbol diagnostics
       -t or v      display filename when processed
       -u<symbol>   make symbol undefined
       -w<n>        suppress messages above warning level 'n'
       file         any file with .out .o or .a extension

Options and files may appear in any order. 
