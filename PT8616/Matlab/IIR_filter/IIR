function[out_array]= IIR(in_array)
  temp_array=zeros(in_array, size(in_array));
  
  IIR_val=in_array(1);
  
  for index=2:size(temp_array);
    IIR_val=(IIR_val+in_array(index-1))/2;
    temp_array(index)=IIR_val;
  end;
end function;
