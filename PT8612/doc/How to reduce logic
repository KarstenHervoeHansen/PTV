PT8612 Suggestions for saving logic:

Move system parameters look-up-table into block RAM

Move clapboard parameters into block RAM

The above parameter LUT's could be shared between the four instances of HD_Gen_Channel.
A lookup could be done only when changing the respective parameters, e.g. cs change or data_valid signals from serial interface.
